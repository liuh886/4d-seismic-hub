source "https://rubygems.org"

# Declare the dependencies needed to build this site locally. When built
# by GitHub Pages, these gems will be installed automatically. The
# `github-pages` gem includes Jekyll and a curated set of plugins.
gem "github-pages", group: :jekyll_plugins
gem "jekyll-include-cache"
